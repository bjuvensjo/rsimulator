This implementation presupposes e.g.
    that a request consists of a header and a body
    that the header has a specific length
    that the length of the body is specified at a specific position in the header

To use this module, fork and adapt it to your needs :).